apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: main
  name: hashgenerator-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashgenerator
  template:
    metadata:
      labels:
        app: hashgenerator
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: generator
          image: kekalainen/uh-devops-with-kubernetes:1.10-generator
          volumeMounts:
            - name: data-volume
              mountPath: /usr/src/app/data
        - name: server
          image: kekalainen/uh-devops-with-kubernetes:2.06
          envFrom:
            - configMapRef:
                name: hashgenerator-configmap
          volumeMounts:
            - name: data-volume
              mountPath: /usr/src/app/data
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: hashgenerator-persistent-volume-claim