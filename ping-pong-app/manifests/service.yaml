apiVersion: v1
kind: Service
metadata:
  namespace: main
  name: ping-pong-service
spec:
  type: LoadBalancer
  selector:
    app: ping-pong
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080